name: voice2action
description: AI Voice to Action - Hackathon MVP
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  flutter_dotenv: ^5.1.0
  firebase_core: ^2.24.0
  cloud_firestore: ^4.13.0
  intl: ^0.18.1
  url_launcher: ^6.2.1
  path_provider: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sample_audio.m4a
    - .env
